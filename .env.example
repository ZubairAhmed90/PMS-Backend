# ─── Required ─────────────────────────────────────────────
DB_HOST=h40.eu.core.hostnext.net
DB_PORT=3306
DB_USER=petzonep_zubairahmed
DB_PASSWORD=kaimkhankhani@123
DB_NAME=petzonep_PMS-Caregiver
JWT_SECRET=change-me-to-a-random-string-in-production
JWT_EXPIRES_IN=24h
NODE_ENV=production
CORS_ORIGINS=https://yourdomain.com,http://localhost:5173

# ─── Optional: Redis (for multi-replica Socket.io) ────────
# Leave blank or remove for single-server cPanel deployment
# REDIS_URL=redis://localhost:6379

# ─── Optional: MQTT (for IoT device simulator) ────────────
# Leave blank or remove if not running a simulator
# MQTT_BROKER_URL=mqtt://localhost:1883

# ─── Optional: AI risk scoring ────────────────────────────
# DASHSCOPE_API_KEY=your-dashscope-api-key-here
